{%- comment -%}

function to get tag attribute

{%- endcomment -%}
{%- case include.attribute -%}
{%- when 'label_id' -%}
    {%- assign tag_label_id = include.tag_id | replace: ' ', '-' | downcase -%}
    {{ tag_label_id }}
{%- when 'is_tag_show' -%}
    {%- if include.tag_id != '' -%}
    show
    {%- endif -%}
{%- else -%}
    {% include error-can-not-case-include.attribute-in-get_tag_attribute %}
{%- endcase -%}